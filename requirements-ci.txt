# CI-only requirements: CPU-only, headless, minimal.
# Excludes heavy/GPU deps (aitviewer/OpenGL, sentence-transformer model weights).
# Tests that touch those paths are marked `slow` and excluded from CI.

# -- Core utilities -------------------------------------------------------
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.65.0
joblib>=1.3.0

# -- Deep learning (CPU build; installed via --extra-index-url in workflow) -
torch>=2.0.0

# -- Motion & language ----------------------------------------------------
scipy>=1.11.0
sentence-transformers>=2.2.0
spacy>=3.7.0

# -- Render (import-only in CI; rendering tests are marked slow and skipped) -
aitviewer>=0.9.0

# -- Runtime safety -------------------------------------------------------
beartype>=0.18.0

# -- Testing --------------------------------------------------------------
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-xdist>=3.5.0
