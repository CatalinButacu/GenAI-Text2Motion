# Requirements
# Install: pip install -r requirements.txt

# -- Core utilities -------------------------------------------------------
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.65.0
joblib>=1.3.0

# -- Deep learning --------------------------------------------------------
torch>=2.0.0

# -- 3D rendering (SMPL-X) ------------------------------------------------
aitviewer>=0.9.0

# -- Motion & language ----------------------------------------------------
scipy>=1.11.0
sentence-transformers>=2.2.0
spacy>=3.7.0

# -- Data download scripts ------------------------------------------------
gdown>=4.7.0
huggingface_hub>=0.20.0
requests>=2.31.0

# -- Evaluation metrics (nltk/rouge used in FID + BLEU scripts) -----------
nltk>=3.8.0
rouge-score>=0.1.2

# -- Runtime safety -------------------------------------------------------
beartype>=0.18.0

# -- Experiment tracking (offline by default) ----------------------------
wandb>=0.15.0

# -- Profiling (opt-in via PROFILE_MEMORY=1) ------------------------------
memory-profiler>=0.61.0

# -- Testing & tooling ----------------------------------------------------
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-xdist>=3.5.0
ruff>=0.1.0
pyright>=1.1.400
