# data & licensed assets — never commit (290 GB donor corpus, SMPL-X bodies).
# Root-anchored: the unanchored `data/` silently ignored src/text2motion/data/ (found 2026-06-10).
/data/
/checkpoints/
/outputs/
*.zip
*.npz
*.pkl

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/

# caches
.cache/
/data/.cache/

# machine-local Claude Code settings
.claude/settings.local.json

# editor
.vscode/
.idea/

# aws ssm scratch (command docs / ids)
_*.json
_cmdid.txt

# secrets & terraform state (infra/aws) + the S3 bundle
*.pem
*.pub
bundle.tar
infra/aws/.terraform/
infra/aws/*.tfstate
infra/aws/*.tfstate.*
infra/aws/terraform.tfvars
infra/aws/.terraform.lock.hcl
