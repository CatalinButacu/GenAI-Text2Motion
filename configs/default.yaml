seed: 2026
device: cuda

# Primary track: standard HumanML3D-263 (citable FID). 20 fps is the standard (Guo/T2M-GPT/MoMask).
hml3d:
  dim: 263
  num_joints: 22
  fps: 20

data:
  track: hml3d263          # "hml3d263" (primary) | "smplx168" (deferred demo)
  sources: [humanml3d]
  mirror_augment: true
  max_motion_len: 196
  min_motion_len: 40

# Reuse from the donor (read-only). Override per machine via env or a local config.
paths:
  donor_root: D:\Facultate\dissertation
  humanml3d_dir: D:\Facultate\dissertation\data\humanml3d
  eval_matcher: D:\Facultate\dissertation\data\t2m\text_mot_match\model\finest.tar
  # Canonical standard-263 eval stats + T2M-GPT VQ-VAE (for harness validation). NOTE: the donor's
  # data/humanml3d motion_data is 272-dim (non-standard) â€” do NOT feed it to the 263 evaluator.
  eval_stats_dir: D:\Facultate\dissertation\data\t2m_download\extracted\t2m\Comp_v6_KLD005\meta
  t2m_vqvae: D:\Facultate\dissertation\data\t2m_download\extracted\t2m\VQVAEV3_CB1024_CMT_H1024_NRES3
  glove_dir: D:\Facultate\dissertation\data\inter-x\misc
  amass_dir: D:\Facultate\dissertation\data\amass
  smplx_models: D:\Facultate\dissertation\data\arctic\unpack\models\smplx
  stats_dir: D:\Facultate\dissertation\data\stats
  cache_dir: data/.cache
  checkpoints_dir: checkpoints
  outputs_dir: outputs
  # Standard HumanML3D-263 regeneration (src/text2motion/data/hml3d). SMPL+H + DMPL are the official
  # AMASS-extended body models (license-gated, not committed). Layout: {male,female}/model.npz.
  body_model_dir: D:\Facultate\dissertation\data\body_models
  smplh_dir: D:\Facultate\dissertation\data\body_models\smplh
  dmpl_dir: D:\Facultate\dissertation\data\body_models\dmpls
  hml3d_index_csv: D:\Facultate\dissertation\data\HumanML3D\index.csv
  # Citable track = the official 263 (TeoGchx/HumanML3D mirror), converted to our layout. Our own
  # SMPL-X regeneration lives at data\HumanML3D_263 (kept for the deferred 168 whole-body demo).
  hml3d_out_dir: data\HumanML3D_official
  texts_dir: D:\Facultate\dissertation\data\HumanML3D\texts   # official <id>.txt + M<id>.txt captions

# Secondary track (deferred 168 SMPL-X studio demo). Layout matches the donor exactly.
avatar:
  model_type: smplx
  gender: neutral
  use_pca: false
  up_axis: z               # representation Z-up; render converts to Y-up
  models_path: D:\Facultate\dissertation\data\arctic\unpack\models\smplx
