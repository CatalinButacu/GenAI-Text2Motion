seed: 2026
device: cuda

data:
  track: hml3d263
  sources: [humanml3d]
  mirror_augment: true
  max_motion_len: 196
  min_motion_len: 40

paths:
  donor_root: null
  humanml3d_dir: null
  eval_matcher: data/official_evaluator/extracted/t2m/text_mot_match/model/finest.tar

  eval_stats_dir: data/eval_stats
  t2m_vqvae: null
  glove_dir: data/t2m_glove
  amass_dir: null
  smplx_models: data/smplx_models
  stats_dir: data/eval_stats
  cache_dir: data/.cache
  checkpoints_dir: checkpoints
  outputs_dir: outputs
  tokenizer_checkpoint_path: checkpoints/tokenizer/fsq_g8_v1024.pt
  generator_checkpoint_paths:
    transformer: checkpoints/generator/generator_transformer_bs8_enc_last.pt
    mamba: checkpoints/generator/generator_mamba_bs8_last.pt

  body_model_dir: null
  smplh_dir: null
  dmpl_dir: null
  hml3d_index_csv: null

  hml3d_out_dir: data/HumanML3D_official
  texts_dir: data/HumanML3D_official/texts

avatar:
  model_type: smplx
  gender: neutral
  use_pca: false
  up_axis: z
  models_path: data/smplx_models

service:
  bind_host: 127.0.0.1
  connect_host: 127.0.0.1
  port: 8765
  idle_seconds: 600
